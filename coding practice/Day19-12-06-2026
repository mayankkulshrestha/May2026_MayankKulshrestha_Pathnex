# Day 19 — Git Deploy, RDS Skeleton, Rolling Strategy, Ping Check

## 🔹 Ansible — Deploy App Using Git

---
- name: Deploy app from git for Pathnex
  hosts: all
  become: yes

  tasks:
    - name: Install Git
      yum:
       name: git
       state: present 

    - name: clone repo
      git:
        repo: https://github.com/pathnex/app.git"
        dest: "/opt/pathnex-app"


## 🔹 Terraform — RDS Skeleton (MySQL)

resource "aws_db_instance" "PathnexRDS" {
  allocated_storage      = 20
  engine                 = "mysql"
  instance_class         = "db.t3.micro"
  username               = "admin"
  password               = "Pathnex123"
  skip_final_snapshot    = false
}


## 🔹 Kubernetes — Deployment Rolling Update (Detailed)

apiVersion: apps/v1
kind: Deployment
metadata:
  name: pathnex-rollout
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 1
  selector: #Deployment un Pods ko manage karega jin par label ho
    matchLabels:
      app: rollout
  template:  #Yeh define karta hai ki naye Pods kaise banenge.
    metadata:
      labels:
        app: rollout
    spec:
      containers:
        - name: app
          image: nginx


##Rule yaad rakho:

#spec.selector.matchLabels aur spec.template.metadata.labels match karne chahiye.

#Ye Kubernetes Deployment interviews mein bhi bahut poocha jata hai:
#"Why are labels required in the pod template?"
#Answer: Deployment selector ko Pods identify aur manage karne ke liye matching labels chahiye hote hain.

# Approvals

## 🔹 Jenkins Pipeline — Manual Approval
You will learn how to **pause pipeline until manual approval**.

pipeline {
    agent any
    environment {
        INSTITUTE_NAME = "Pathnex"
    }
    stages {
        stage('Deploy') {
            steps {
                input message: "Approve deployment for $INSTITUTE_NAME?"
                sh 'echo "Deployment approved and executed!"'
            }
        }
    }
}

## 🔹 GitLab CI — Manual Approval Job
You will learn how to **use manual jobs in GitLab CI for approvals**.

stages:
  - deploy

variables:
  INSTITUTE_NAME: "Pathnex"

deploy:
  stage: deploy
  image: alpine:latest
  script:
    - echo "Ready to deploy $INSTITUTE_NAME"
  when: manual